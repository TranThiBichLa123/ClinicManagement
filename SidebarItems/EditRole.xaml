<Window x:Class="ClinicManagement.SidebarItems.EditRole"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClinicManagement.SidebarItems"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Cập nhật nhóm quyền" Height="600" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tên nhóm quyền -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Mã nhóm quyền" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="RoleCodeTextBox" Width="258" Height="30"/>
            <TextBlock Text="Tên nhóm quyền" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="RoleNameTextBox" Width="291" Height="30"/>

        </StackPanel>
        <DataGrid Grid.Row="1"
          RowStyle="{DynamicResource DataGridRowStyle1}"
          ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
          CellStyle="{DynamicResource DataGridCellStyle1}"
          x:Name="membersDataGrid"
          Style="{DynamicResource DataGridStyle1}"
          Margin="10">
            <DataGrid.Columns>
                <!-- Cột văn bản đầu tiên -->
                <DataGridTextColumn Header="Danh mục chức năng" Binding="{Binding ChucNang}" IsReadOnly="True" Width="2*" />

                <!-- Cột checkbox: Xem -->
                <DataGridCheckBoxColumn Header="Xem" Binding="{Binding Xem}" Width="*" ElementStyle="{StaticResource CheckBoxStyle1}">
                    <DataGridCheckBoxColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox Style="{StaticResource CheckBoxStyle1}" />
                        </DataTemplate>
                    </DataGridCheckBoxColumn.HeaderTemplate>
                </DataGridCheckBoxColumn>

                <!-- Cột checkbox: Tạo mới -->
                <DataGridCheckBoxColumn Header="Tạo mới" Binding="{Binding TaoMoi}" Width="*" ElementStyle="{StaticResource CheckBoxStyle1}">
                    <DataGridCheckBoxColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox Style="{StaticResource CheckBoxStyle1}" />
                        </DataTemplate>
                    </DataGridCheckBoxColumn.HeaderTemplate>
                </DataGridCheckBoxColumn>

                <!-- Cột checkbox: Cập nhật -->
                <DataGridCheckBoxColumn Header="Cập nhật" Binding="{Binding CapNhat}" Width="*" ElementStyle="{StaticResource CheckBoxStyle1}">
                    <DataGridCheckBoxColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox Style="{StaticResource CheckBoxStyle1}" />
                        </DataTemplate>
                    </DataGridCheckBoxColumn.HeaderTemplate>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Nút thao tác -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Cập nhật nhóm ..." Width="150" Height="35" Margin="5" Background="#2196F3" Foreground="White"/>
            <Button Content="Huỷ bỏ" Width="100" Height="35" Margin="5" Background="#E53935" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
