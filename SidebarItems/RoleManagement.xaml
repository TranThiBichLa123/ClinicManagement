<UserControl x:Class="ClinicManagement.SidebarItems.RoleManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sbi="clr-namespace:ClinicManagement.SidebarItems"
             xmlns:local="clr-namespace:ClinicManagement.SidebarItems"
             mc:Ignorable="d" 
                          Loaded="UserControl_Loaded"

             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="620" d:DesignWidth="1200">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <Style x:Key="AccountRowStyle" TargetType="DataGridRow">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="MinHeight" Value="48"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Grid Margin="0 2">
                            <!-- 👈 Giãn dòng -->
                            <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            CornerRadius="10"
                            SnapsToDevicePixels="True">
                                <DataGridCellsPresenter/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2ca3eb"/>
                                <!-- xanh nhạt -->
                            </Trigger>



                            <!-- Vô hiệu hóa -->
                            <DataTrigger Binding="{Binding IsDisabled}" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#fef3dc"/>
                                <!-- màu vàng be -->
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>

                            <!-- Ngăn hover đè màu vô hiệu hóa -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsDisabled}" Value="True"/>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#fef3dc"/>
                            </MultiDataTrigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>





    </UserControl.Resources>


    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="1, 0" EndPoint="0, 1">
                <GradientStop Color="White" Offset="0"/>
                <GradientStop Color="Silver" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Top" Height="45">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0, 0" EndPoint="1, 1">
                    <GradientStop Color="#29A3EC" Offset="0"/>
                    <GradientStop Color="#B3D8A8" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <materialDesign:PackIcon Kind="Share" Width="28" Height="28" Foreground="#CC000000" Margin="0,7,5,0"/>
                <TextBlock
        FontSize="30" FontWeight="Bold" HorizontalAlignment="Left"
FontFamily="Segoe UI" Foreground="#CC000000"
VerticalAlignment="Bottom" Margin="0,0,0,0"
        Text="Phân quyền người dùng"/>
            </StackPanel>



        </Grid>


        <TabControl Grid.Row="1" Background="Transparent"             x:Name="TabControl_Setting">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="Gainsboro" CornerRadius="0,0,0,0" Margin="0,0">
                                    <ContentPresenter x:Name="ContentSite"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                ContentSource="Header"
                                Margin="10,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="LightSkyBlue"  />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="GhostWhite" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem IsSelected="True">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Account" Width="20" Height="20" Margin="0,0,5,0" Foreground="DarkSlateBlue"/>
                        <TextBlock Text="Tài khoản" FontWeight="Bold" VerticalAlignment="Center" Foreground="DarkSlateBlue"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Background="Transparent">
                    <Border CornerRadius="0,0,20,20">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="1,0" EndPoint="0,1">
                                <GradientStop Color="White" Offset="0"/>
                                <GradientStop Color="Silver" Offset="1.5"/>
                            </LinearGradientBrush>
                        </Border.Background>

                        <DataGrid Grid.Row="1" x:Name="accountDataGrid" RowStyle="{StaticResource AccountRowStyle}" AutoGenerateColumns="False" IsReadOnly="True" RowHeaderWidth="0" Style="{DynamicResource DataGridStyle1}" ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle1}" CellStyle="{StaticResource DataGridCellStyle1}" SelectionChanged="accountDataGrid_SelectionChanged" Margin="10,10,10,10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã tài khoản" Binding="{Binding ID_TaiKhoan}" Width="*"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                                <DataGridTextColumn Header="Mật khẩu" Binding="{Binding MatKhau}" Width="*"/>
                                <DataGridTextColumn Header="Mã vai trò" Binding="{Binding ID_VaiTro}" Width="*"/>
                                <DataGridTextColumn Header="Trạng thái tài khoản" Binding="{Binding TrangThaiTaiKhoan}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ShareAll" Width="20" Height="20" Margin="0,0,5,0" Foreground="DarkSlateBlue"/>
                        <TextBlock Text="Phân quyền" FontWeight="Bold" VerticalAlignment="Center" Foreground="DarkSlateBlue"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Background="Transparent">
                    <Border CornerRadius="0,0,20,20">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="1,0" EndPoint="0,1">
                                <GradientStop Color="White" Offset="0"/>
                                <GradientStop Color="Silver" Offset="1.5"/>
                            </LinearGradientBrush>
                        </Border.Background>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10 0 30 10">
                                <Button Content="➕ Thêm" Style="{StaticResource topMenuButton}" Click="btnAddRole_Click"/>
                                <Button Content="❌ Xóa" Style="{StaticResource topMenuButton}" Click="btnDelete_Click"/>
                            </StackPanel>

                            <!-- DataGrid -->
                            <DataGrid Grid.Row="1" 
          RowStyle="{DynamicResource DataGridRowStyle1}"
          ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
          CellStyle="{DynamicResource DataGridCellStyle1}"
                                      Style="{DynamicResource DataGridStyle1}"
          x:Name="roleDataGrid"
                               
          Margin="10,10,10,10">
                                <!-- Thêm margin để không dính sát -->
                                <DataGrid.Columns>
                                    <DataGridCheckBoxColumn   Binding="{Binding IsSelected, Mode=TwoWay}" Header="Chọn" CanUserResize="False" Width="auto" ElementStyle="{StaticResource CheckBoxStyle1}">

                                        <DataGridCheckBoxColumn.HeaderTemplate>
                                            <DataTemplate>
                                                <CheckBox Style="{StaticResource CheckBoxStyle1}"   />
                                            </DataTemplate>
                                        </DataGridCheckBoxColumn.HeaderTemplate>
                                    </DataGridCheckBoxColumn>



                                    <DataGridTextColumn Header="Mã quyền" Binding="{Binding ID_VaiTro}" IsReadOnly="True" Width="*"/>
                                    <DataGridTextColumn Header="Tên nhóm quyền" Binding="{Binding TenVaiTro}" IsReadOnly="True" Width="*"/>

                                    <DataGridTemplateColumn Header="Hành động" Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="✏️"
                        Style="{StaticResource topMenuButton}"
                        Width="30"
                        Height="30"
                        ToolTip="Chỉnh sửa quyền"
                        Click="EditRoleButton_Click"
                        Tag="{Binding ID_VaiTro}" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Eye" Width="20" Height="20" Margin="0,0,5,0" Foreground="DarkSlateBlue"/>
                        <TextBlock Text="Giám sát và kiểm soát truy cập" FontWeight="Bold" VerticalAlignment="Center" Foreground="DarkSlateBlue"/>
                    </StackPanel>
                </TabItem.Header>

                <Grid Background="Transparent">
                    <Border CornerRadius="0,0,20,20">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="1,0" EndPoint="0,1">
                                <GradientStop Color="White" Offset="0"/>
                                <GradientStop Color="Silver" Offset="1.5"/>
                            </LinearGradientBrush>
                        </Border.Background>

                        <!-- Chèn UserControl ở đây -->
                        <sbi:SuspiciousLoginMonito />
                    </Border>
                </Grid>
            </TabItem>


        </TabControl>
    </Grid>
</UserControl>
