<Window x:Class="ClinicManagement.SidebarItems.NewDrug"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClinicManagement.SidebarItems"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d" 
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Loaded="Window_Loaded" Background="Transparent"
     WindowStyle="None" AllowsTransparency="True"
        Height="585" Width="1390" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </Window.Resources>
    <Border CornerRadius="15" BorderThickness="2" Background="White">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="3,0">
                <GradientStop Color="#29A3EC" Offset="0"/>
                <GradientStop Color="#FF65CBEE" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Grid>
            <Grid.Background>
                <ImageBrush/>
            </Grid.Background>

            <!-- Chia 4 cột -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="270" />
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="530"/>
            </Grid.ColumnDefinitions>

            <!-- Chia 6 hàng -->
            <Grid.RowDefinitions>
                <RowDefinition Height="76" />
                <RowDefinition Height="20"/>
                <RowDefinition Height="76" />
                <RowDefinition Height="76" />
                <RowDefinition Height="76" />
                <RowDefinition Height="76" />
                <RowDefinition Height="76" />
                <RowDefinition Height="76" />
                <RowDefinition Height="76" />


            </Grid.RowDefinitions>
            <Border Grid.Row="0" Grid.ColumnSpan="4"
CornerRadius="15,15,0,0" Margin="0,0,-26,0">
                <Border.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FF29A3EC" Offset="0"/>
                        <GradientStop Color="#FF65CBEE" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid >
                    <TextBlock Text="Phiếu nhập thuốc"
           Foreground="White"
           FontWeight="Bold"
           FontSize="36"
           VerticalAlignment="Center"
           HorizontalAlignment="Center"/>

                    <!-- Nút đóng -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,10,0" Height="30" Orientation="Horizontal" FlowDirection="RightToLeft" Grid.RowSpan="2">
                        <Button x:Name="btnClose" Style="{StaticResource IconButtonsStyle}" Content="{DynamicResource close}" Click="btnClose_Click" Tag="IsCloseButton"/>
                       
                        <Button x:Name="btnMinimize" Style="{DynamicResource IconButtonsStyle}" Content="{DynamicResource minimize}" Click="btnMinimize_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- 
Ảnh hiển thị thuốc -->
            <StackPanel Grid.RowSpan="6"
      VerticalAlignment="Top" HorizontalAlignment="Center"
      Margin="0,62,0,0" Height="376" Width="300" Grid.Row="2">
                <!-- Ảnh thuốc -->
                <Border Width="245" Height="245"
  CornerRadius="15"
  ClipToBounds="True">
                    <Image x:Name="imgThuoc"
     Stretch="UniformToFill"
     Source="/img/drugDefault.jpg"/>
                </Border>


                <!-- Nút chọn ảnh -->
                <Button Name="btnChonAnhThuoc" Content="Chọn ảnh thuốc"  Height="40"
      Foreground="White" FontSize="10" FontWeight="Bold" Margin="0,15,0,0"
     HorizontalAlignment="Center" Cursor="Hand" BorderThickness="0"
      Width="240" Click="NewDrugImg_Click" >
                    <Button.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FF65CBEE" Offset="0"/>
                            <GradientStop Color="#FF29A3EC" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Background>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                              CornerRadius="4"
                              Padding="10"
                              BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>


            <Grid Grid.Row="2" Grid.Column="2" Margin="0,0,0,0">
                <Border CornerRadius="5"
            BorderThickness="2"
            BorderBrush="Silver"
            Background="White"
            Width="250"
            Height="50"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
                    <ComboBox x:Name="DvtcomboBox"
                  DisplayMemberPath="TenDVT"
                  SelectedValuePath="ID_DVT"
                  SelectedValue="{Binding SelectedDVT_ID, Mode=TwoWay}"
                  ItemsSource="{Binding DanhSachDVT}"
                  Background="Transparent"
                  BorderThickness="0"
                  Height="35"
                  IsEditable="False"
                  FontSize="14"
                  VerticalContentAlignment="Center"
                  Margin="8,7,8,4"/>
                </Border>
                <TextBlock Text="Nhập đơn vị tính"
               Foreground="Gray"
               Margin="35,0,45,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False"
               Visibility="{Binding ElementName=DvtcomboBox, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>
            </Grid>


            <Grid Grid.Row="3" Grid.Column="1">
                <Border CornerRadius="5"
            BorderThickness="2"
            BorderBrush="Silver"
            Background="White"
            Width="250"
            Height="50"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
                    <ComboBox x:Name="CachDungcomboBox"
                  DisplayMemberPath="MoTaCachDung"
                  SelectedValuePath="ID_CachDung"
                  SelectedValue="{Binding SelectedCachDung_ID, Mode=TwoWay}"
                  ItemsSource="{Binding DanhSachCachDung}"
                  Background="Transparent"
                  BorderThickness="0"
                  Height="35"
                  IsEditable="False"
                  FontSize="14"
                  VerticalContentAlignment="Center"
                  Margin="8,7,8,4"/>
                </Border>
                <TextBlock Text="Nhập cách dùng"
               Foreground="Gray"
               Margin="25,0,45,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False"
               Visibility="{Binding ElementName=CachDungcomboBox, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>
            </Grid>


            <Grid Grid.Row="3" Grid.Column="2">
                <!-- Border bọc ComboBox để tạo viền bo góc -->
                <Border CornerRadius="5"
    BorderThickness="2"
    BorderBrush="Silver"
    Background="White"
    Width="250"
    Height="50" VerticalAlignment="Center" HorizontalAlignment="Center">

                    <!-- ComboBox nhập -->
                    <ComboBox x:Name="ThanhPhancomboBox"
          Background="Transparent"
          BorderThickness="0"
          Height="35"
          IsEditable="True"
          IsTextSearchEnabled="True"
          FontSize="14"
          VerticalContentAlignment="Center"
                                    ItemsSource="{Binding DanhSachThanhPhan }"

          SelectedItem="{Binding ThanhPhan, Mode=TwoWay}" Margin="8,7,8,4">

                    </ComboBox>
                </Border>

                <!-- TextBlock đóng vai trò placeholder -->
                <TextBlock Text="Nhập thành phần"
       Foreground="Gray"
       Margin="35,0,45,0"
       VerticalAlignment="Center"
       IsHitTestVisible="False"
       Visibility="{Binding ElementName=ThanhPhancomboBox, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>
            </Grid>
            <Grid Grid.Row="4" Grid.Column="2">
                <!-- Border bọc ComboBox để tạo viền bo góc -->
                <Border CornerRadius="5"
    BorderThickness="2"
    BorderBrush="Silver"
    Background="White"
    Width="250"
    Height="50" VerticalAlignment="Center" HorizontalAlignment="Center">

                    <!-- ComboBox nhập -->
                    <ComboBox x:Name="XuatXucomboBox"
          Background="Transparent"
          BorderThickness="0"
          Height="35"
          IsEditable="True"
          IsTextSearchEnabled="True"
          FontSize="14"
          VerticalContentAlignment="Center"
                                 ItemsSource="{Binding DanhSachXuatXu}"
   
                          
          SelectedItem="{Binding XuatXu, Mode=TwoWay}" Margin="8,7,8,4">
                    </ComboBox>
                </Border>

                <!-- TextBlock đóng vai trò placeholder -->
                <TextBlock Text="Nhập xuất xứ"
       Foreground="Gray"
       Margin="35,0,45,0"
       VerticalAlignment="Center"
       IsHitTestVisible="False"
       Visibility="{Binding ElementName=XuatXucomboBox, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>
            </Grid>

            <Border Grid.Row="5" Grid.Column="1"
        Height="50"
        BorderBrush="Silver"
        BorderThickness="2"
        CornerRadius="4"
        Margin="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <!-- Text -->
                        <ColumnDefinition />
                        <!-- Icon -->
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <!-- Placeholder -->
                    <Grid>
                        <TextBlock Text="Nhập số lượng"
                       Foreground="Silver"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Panel.ZIndex="1"
                       IsHitTestVisible="False"
                       Visibility="{Binding ElementName=textBoxSoLuongNhap, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>

                        <TextBox Name="textBoxSoLuongNhap"
                     BorderThickness="0"
                     Foreground="Black"
                     VerticalAlignment="Center"
                     FontWeight="SemiBold"
                     Margin="10,0,0,0"/>
                    </Grid>

                    <!-- Icon -->
                    <fa:IconImage 
            Grid.Column="1"
            Width="25"
            Height="25"
Icon="Boxes"
                    Style="{DynamicResource menuButtonIcon}"
            Foreground="Silver"
            HorizontalAlignment="Right"
            Margin="0,0,10,0"/>
                </Grid>
            </Border>
            <Border Grid.Row="4" Grid.Column="1"
        Height="50"
        BorderBrush="Silver"
        BorderThickness="2"
        CornerRadius="4"
        Margin="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <!-- Text -->
                        <ColumnDefinition />
                        <!-- Icon -->
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <!-- Placeholder -->
                    <Grid>
                        <TextBlock Text="Nhập đơn giá nhập"
                       Foreground="Silver"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Panel.ZIndex="1"
                       IsHitTestVisible="False"
                       Visibility="{Binding ElementName=textBoxDonGiaNhap, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>

                        <TextBox Name="textBoxDonGiaNhap"
                     BorderThickness="0"
                     Foreground="Black"
                     VerticalAlignment="Center"
                     FontWeight="SemiBold"
                     Margin="10,0,0,0"
                             />
                    </Grid>

                    <!-- Icon -->
                    <fa:IconImage 
            Grid.Column="1"
            Width="25"
            Height="25"
    Icon="DollarSign"
            Style="{DynamicResource menuButtonIcon}"
            Foreground="Silver"
            HorizontalAlignment="Right"
            Margin="0,0,10,0"/>
                </Grid>
            </Border>
            <Border Grid.Row="5" Grid.Column="2"
        Height="50"
        BorderBrush="Silver"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
        BorderThickness="2"
        CornerRadius="4"
        Width="250">
                <Grid>


                    <!-- DatePicker thay vì TextBox -->
                    <DatePicker Name="datePickerHanSuDung"
                    BorderThickness="0"
                    Foreground="black"
                    FontWeight="SemiBold"
                    VerticalContentAlignment="Center"
                    SelectedDate="{Binding HanSuDung, Mode=TwoWay}"
                    Margin="10,0,0,0"
                    Background="Transparent"
                    />


                </Grid>
            </Border>
            <Button Name="btnReset" Grid.Column="1" Grid.Row="7" Content="Mặc định"  Height="40"
Foreground="White" FontSize="10" FontWeight="Bold"  Click="Reset_Click"
 Cursor="Hand" BorderThickness="0"  Margin="20,18,20,18" >
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FFF15C08" Offset="0"/>
                        <GradientStop Color="#FFF31111" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="4"
            Padding="10"
            Background="{TemplateBinding Background}"
            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Button Name="btnChonThuoc" Grid.Column="2" Grid.Row="7" Content="Chọn thuốc"  Height="40" 
Foreground="White" FontSize="10" FontWeight="Bold"  Click="ChonThuoc_Click"
 Cursor="Hand" BorderThickness="0"  Margin="20,0,20,0" >
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FF65CBEE" Offset="0"/>
                        <GradientStop Color="#FF29A3EC" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                        CornerRadius="4"
                        Padding="10"
                        BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Button Name="btnNhapThuoc" Grid.Column="3" Grid.Row="7" Content="Nhập thuốc"  Height="40"
 Foreground="White" FontSize="10" FontWeight="Bold" 
  Cursor="Hand" BorderThickness="0" Click="addDrug_Click" Margin="20,0,20,0" >
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FF65CBEE" Offset="0"/>
                        <GradientStop Color="#FF29A3EC" Offset="1"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                         CornerRadius="4"
                         Padding="10"
                         BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Border Grid.Row="6" Grid.Column="1"
        Height="50"
        BorderBrush="Silver"
        BorderThickness="2"
        CornerRadius="4"
        Margin="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <!-- Text -->
                        <ColumnDefinition />
                        <!-- Icon -->
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <!-- Placeholder -->
                    <Grid>
                        <TextBlock Text="Nhập tỷ lệ giá bán"
                       Foreground="Silver"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Panel.ZIndex="1"
                       IsHitTestVisible="False"
                       Visibility="{Binding ElementName=textBoxTyLeGiaBan, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>

                        <TextBox Name="textBoxTyLeGiaBan"
                     BorderThickness="0"
                     Foreground="Black"
                     VerticalAlignment="Center"
                     FontWeight="SemiBold"
                     Margin="10,0,0,0"
                             IsReadOnly="True"
/>
                    </Grid>

                    <!-- Icon -->
                    <fa:IconImage 
            Grid.Column="1"
            Width="25"
            Height="25"
Icon="Percent"
                    Style="{DynamicResource menuButtonIcon}"
            Foreground="Silver"
            HorizontalAlignment="Right"
            Margin="0,0,10,0"/>
                </Grid>
            </Border>

            <Grid Grid.Row="2" Grid.Column="1">
                <!-- Border bọc ComboBox để tạo viền bo góc -->
                <Border CornerRadius="5"
            BorderThickness="2"
            BorderBrush="Silver"
            Background="White"
            Width="250"
            Height="50" VerticalAlignment="Center" HorizontalAlignment="Center">

                    <!-- ComboBox nhập -->
                    <ComboBox x:Name="TenThuoccomboBox"
                          LostFocus="TenThuoccomboBox_LostFocus"

                          Background="Transparent"
                  BorderThickness="0"
                  Height="35"
                  IsEditable="True"
                  IsTextSearchEnabled="True"
                  FontSize="14"
                  VerticalContentAlignment="Center"
          ItemsSource="{Binding DanhSachTenThuoc}"

                  SelectedItem="{Binding TenThuoc, Mode=TwoWay}" Margin="8,7,8,4">

                    </ComboBox>
                </Border>

                <!-- TextBlock đóng vai trò placeholder -->
                <TextBlock Text="Nhập tên thuốc"
               Foreground="Gray"
               Margin="25,0,45,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False"
               Visibility="{Binding ElementName=TenThuoccomboBox, Path=Text.IsEmpty, Converter={StaticResource BoolToVisConverter}}"/>
            </Grid>
            <ScrollViewer Grid.Column="3"
                         
              Grid.RowSpan="5"
              HorizontalScrollBarVisibility="Auto"
              VerticalScrollBarVisibility="Disabled"
              CanContentScroll="False" Margin="0,0,-26,13" Grid.Row="2">
                <Border CornerRadius="15" BorderThickness="2" Width="538" >
                    <Border.BorderBrush>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="3,0">
                            <GradientStop Color="#29A3EC" Offset="0"/>
                            <GradientStop Color="#FF65CBEE" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.BorderBrush>
                    <DataGrid x:Name="drugDataGrid"
            
                          RowStyle="{DynamicResource DataGridRowStyle1}"
              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}"
              CellStyle="{DynamicResource DataGridCellStyle1}"
              Style="{DynamicResource DataGridStyle1}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              AlternationCount="2000"
              HeadersVisibility="Column"
              CanUserAddRows="False"
              BorderThickness="0"
              Background="White"
              HorizontalScrollBarVisibility="Disabled"
              ScrollViewer.CanContentScroll="False"
              Width="520" Height="347">
                        <DataGrid.Columns>

                            <!-- STT -->
                            <DataGridTemplateColumn Header="#" Width="40" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=AlternationIndex, Converter={StaticResource RowIndexConverter}}"
                     HorizontalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Ảnh + tên thuốc -->
                            <DataGridTemplateColumn Header="Tên thuốc" IsReadOnly="True" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="35" Height="35" Margin="0 0 10 0">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding HinhAnh}" Stretch="UniformToFill"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <TextBlock Text="{Binding TenThuoc}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Số lượng -->
                            <DataGridTextColumn Header="Số lượng nhập" Binding="{Binding SoLuongNhap}" Foreground="#FF5252" IsReadOnly="True" Width="*"/>

                            <!-- Đơn giá nhập -->
                            <DataGridTextColumn Header="Đơn giá nhập" Binding="{Binding DonGiaNhap}" IsReadOnly="True" Width="*"/>

                            <!-- Thành tiền -->
                            <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien}" IsReadOnly="True" Width="*"/>

                            <!-- Hạn sử dụng -->
                            <DataGridTextColumn Header="Hạn sử dụng" Binding="{Binding HanSuDung, StringFormat=dd/MM/yyyy}" IsReadOnly="True" Width="*"/>

                            <!-- Nút sửa -->
                            <DataGridTemplateColumn Header="Sửa" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Margin="0 0 10 0"
                  Style="{StaticResource gridEditButton}"
                  Click="SuaThuoc_Click">
                                            <Icon:PackIconMaterial Kind="PencilOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Nút xóa -->
                            <DataGridTemplateColumn Header="Xóa" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Margin="0 0 10 0"
                  Style="{StaticResource gridRemoveButton}"
                  Click="XoaThuoc_Click">
                                            <Icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </ScrollViewer>

        </Grid>
    </Border>
</Window>
