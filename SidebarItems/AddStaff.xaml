<Window x:Class="ClinicManagement.SidebarItems.AddStaff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm nhân viên" Height="700" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="Transparent" AllowsTransparency="True" WindowStyle="None">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DefaultButton" TargetType="Button">
            <Setter Property="Background" Value="#EEEEEE"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FancyTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="0 0 0 16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" Padding="0" CornerRadius="16" BorderBrush="#DDD" BorderThickness="0" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Headbar with title and close button -->
            <Border Background="#007ACC" CornerRadius="16,16,0,0" Grid.Row="0">
                <Grid MouseLeftButtonDown="Window_MouseLeftButtonDown">
                    <TextBlock Text="Thêm nhân viên" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="16,0,0,0"/>
                    <Button Content="X" Width="32" Height="32" Click="btnClose_Click"
                            Background="Transparent" BorderThickness="0" Foreground="White"
                            HorizontalAlignment="Right" Margin="0 4 6 0" VerticalAlignment="Top"
                            Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Main form content -->
            <Border Grid.Row="1" Padding="30" CornerRadius="0,0,16,16">
                <Grid Margin="0,-22,0,-22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Avatar -->
                    <Grid Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0 0 0 25">
                        <Border Width="120" Height="120" CornerRadius="60" BorderBrush="#AAA" BorderThickness="1.5" Cursor="Hand" MouseLeftButtonUp="AvatarEllipse_MouseLeftButtonUp" ToolTip="Click để chọn ảnh">
                            <Border.Background>
                                <ImageBrush x:Name="avatarBrushAdd" ImageSource="\img\avatar.jpg" Stretch="UniformToFill"/>
                            </Border.Background>
                        </Border>
                    </Grid>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Họ tên:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtHoTenAdd" Style="{StaticResource FancyTextBox}"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtEmailAdd" Style="{StaticResource FancyTextBox}"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="SĐT:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="txtDienThoaiAdd" Style="{StaticResource FancyTextBox}"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Giới tính:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <ComboBox Grid.Row="4" Grid.Column="1" x:Name="cbGioiTinhAdd" Height="32" Padding="6,2" BorderBrush="#CCC" BorderThickness="1" Margin="0 0 0 16">
                        <ComboBoxItem Content="Nam"/>
                        <ComboBoxItem Content="Nữ"/>
                    </ComboBox>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Ngày sinh:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <DatePicker Grid.Row="5" Grid.Column="1" x:Name="dpNgaySinhAdd" Height="32" Padding="6,2" BorderBrush="#CCC" BorderThickness="1" Margin="0 0 0 16"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="CCCD:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Row="6" Grid.Column="1" x:Name="txtCCCDAdd" Style="{StaticResource FancyTextBox}"/>

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Địa chỉ:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Row="7" Grid.Column="1" x:Name="txtDiaChiAdd" Style="{StaticResource FancyTextBox}"/>

                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Vai trò:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <ComboBox Grid.Row="8" Grid.Column="1" x:Name="cbVaiTroAdd" 
          Height="32" Padding="6,2" BorderBrush="#CCC" BorderThickness="1" Margin="0 0 0 16"
          DisplayMemberPath="TenVaiTro"
          SelectedValuePath="ID_VaiTro"/>
                    <TextBlock Grid.Row="9" Grid.Column="0" Text="Mật khẩu:" Margin="0 0 10 16" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox Grid.Row="9" Grid.Column="1" x:Name="txtMatKhauAdd" Style="{StaticResource FancyTextBox}"/>

                    <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 0">
                        <Button Content="Lưu" Width="100" Height="32" Click="btnSaveAdd_Click" Style="{StaticResource PrimaryButton}"/>
                        <Button Content="Hủy" Width="100" Height="32" Click="btnCancelAdd_Click" Style="{StaticResource DefaultButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>